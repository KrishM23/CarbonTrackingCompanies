# Vapor frontend on Netlify (monorepo: app lives in /frontend)
[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

# SPA: send all paths to index.html so /login, /contact, etc. work
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
